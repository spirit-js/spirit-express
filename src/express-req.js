// const express_req = require("../node_modules/express/lib/request")

// module.exports = (request) => {
//   // copy over express methods
//   Object.keys(express_req).forEach((method) => {
//     Object.defineProperty(request, method, Object.getOwnPropertyDescriptor(express_req, method))
//   })

//   return request
// }
